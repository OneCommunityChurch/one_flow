from django.conf.urls.defaults import patterns, include, url
from one_flow.one_info.views import *

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'one_flow.views.home', name='home'),
    # url(r'^one_flow/', include('one_flow.foo.urls')),

    url(r'^$', 'index'),
    url(r'^person$', 'person'),

)
